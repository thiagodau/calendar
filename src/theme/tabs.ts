import { colors } from "./colors";

export const MyThemeTabs = {
  dark: false,
  colors: {
    primary: colors.primary,
    background: colors.secondary,
    card: colors.secondary,
    text: colors.white,
    border: colors.lightGrey,
    notification: colors.primary,
  },
};